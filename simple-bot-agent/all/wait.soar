#### Propose wait for a state-no-change
sp {top-state*propose*wait
   (state <s> ^attribute state
              ^choices none
             -^operator.name wait)
-->
   (write |debug: top state propose wait|)
   (<s> ^operator <o> +)
   (<o> ^name wait)
}
